{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd tchallenge-app && npm install && npm run build",
  "outputDirectory": "tchallenge-app/dist",
  "installCommand": "cd tchallenge-app && npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
